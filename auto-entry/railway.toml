# Legacy Railway config (optional). Production is documented for Google Cloud Run — see DEPLOY-CLOUD-RUN.md
# https://docs.railway.com/reference/build-and-start-commands

[build]
buildCommand = "npm ci && npm run setup && npm run build"

[deploy]
startCommand = "node scripts/production-start.cjs"
