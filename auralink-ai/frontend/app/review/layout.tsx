/**
 * Layout for /review so the segment has a proper layout and error boundary can attach.
 */
export default function ReviewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
